import {createTheme} from '@mui/material/styles'
const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#eb3434",
    },
    secondary: {
      main: "#ccc",
    },
  },
});

export default theme;
